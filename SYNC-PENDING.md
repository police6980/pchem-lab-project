# SYNC-PENDING — 지연 동기화 대기 목록

> 이 파일은 **지연 동기화(deferred sync)**의 대기 큐다.
> 규칙 정의는 `CLAUDE.md` → "파일 관리 규칙 → 3. 지연 동기화" 참조.

연동된 파일(동기화 클러스터)을 한 곳 고칠 때마다 형제 파일을 즉시 전부
재빌드하면 토큰이 낭비된다. 그래서 평소엔 **요청받은 파일만 고치고**, 재동기가
필요한 형제·생성물을 여기에 한 줄로 기록만 해 둔다. 실제 전파·재빌드·검증은
`/sync` 명령(또는 "동기화"·"연동 고쳐")으로 **일괄** 처리하며, 끝난 줄은 삭제한다.

**경고**: 커밋/푸시·세션 종료 직전 아래 목록이 비어있지 않으면, 반쪽 상태가
커밋되지 않도록 먼저 `/sync`를 실행할 것.

## 기록 형식

각 줄은 다음 형식의 체크박스 항목으로 적는다:

```
- [ ] (YYYY-MM-DD) [클러스터] 고친 파일 → 재동기 필요: 형제/생성물 — 한 줄 사유
```

예시(설명용 — 실제 대기 아님):

```
- [ ] (2026-05-23) [검사지(measure)] 검사지-최종문항.html 문항 4 수정
      → 재동기 필요: make_docx.py로 docx 재생성 · 배포본 HTML · google-form-script.gs · 종합문서 링크
```

## 대기 목록

(없음)
