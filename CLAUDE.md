# Claude Code 프로젝트 지침

본 파일은 Claude Code(CC)가 이 레포에서 작업할 때 따를 규약을 모은다.
사용자가 요청하면 CC는 아래 규칙에 따라 자동 처리한다.

---

## 개발 일지 (docs/10-dev-journal.md) 유지 지침

본 프로젝트는 논문 작성을 위한 개발 일지를 `docs/10-dev-journal.md`에 축적한다. 
사용자가 **"개발 일지 작성해줘"** / **"일지 업데이트"** / **"journal update"** 같은 요청을 하면 아래 규칙대로 자동 진행한다.

### 목적
의사결정의 근거, 막힌 지점, 대안의 배제 사유를 시간순으로 보존하여 논문의 
방법론·시스템 설계 섹션의 1차 자료로 활용한다.

### 작성 규칙

1. **파일 위치**: `docs/10-dev-journal.md` 고정. 없으면 신규 생성, 있으면 append.

2. **Phase 구조**: 각 Phase는 `## Phase N — 제목 (날짜 범위)` 헤딩으로 시작. 
   하위에 날짜별 섹션 (`### YYYY-MM-DD — 소제목`).

3. **두 가지 기록 병행**:
   - **날짜별 요약**: `#### 한 일` 블록. 불릿 리스트로 간단히.
   - **주요 의사결정**: `#### 결정: 제목` 블록. 
     아래 4요소 필수 — **배경**, **결정**, **근거**, **배제된 대안**.

4. **날짜 소스 우선순위**:
   1. `git log --all --pretty=format:'%h %ad %s' --date=short` 결과 (실제 커밋 날짜)
   2. 메시지에서 유추 (예: "오늘", "어제")
   3. 추정 시 "(추정)" 표시

5. **새 Phase가 추가될 때**: 기존 내용 건드리지 않고 파일 맨 아래 `---` 구분선 
   + 새 Phase 섹션 append.

6. **기존 Phase에 내용 추가 시**: 해당 Phase 섹션 안 적절한 날짜 위치에 삽입. 
   Phase 마일스톤(마지막 블록)보다 위에 둘 것.

### 표준 실행 순서

사용자가 "일지 작성해줘" / "일지 업데이트" 요청 시:

1. **현황 파악**
```bash
   ls docs/10-dev-journal.md 2>/dev/null && echo "exists" || echo "new"
   git log --all --pretty=format:'%h %ad %s' --date=short | head -30
   git status
```

2. **사용자에게 범위 확인** (단, 요청 메시지에 명시되어 있으면 생략):
   - 이번 일지에 담을 범위는? (예: "오늘 한 작업", "Phase 3 전체", 
     "마지막 커밋 5개", 특정 날짜 범위 등)
   - 추가만 할지, 기존 내용 교정도 할지

3. **초안 작성 후 사용자 확인**: 긴 내용이면 파일에 쓰기 전 요약 먼저 보고. 
   짧으면 바로 작성 후 diff로 보고.

4. **커밋 메시지 형식 고정**:
````
   docs(journal): <범위 요약> — Phase N <subphase>
   
   - <핵심 결정 1>
   - <핵심 결정 2>
   - <핵심 기능 1>
````
   예: `docs(journal): append Step 3-1 emulator skeleton — Phase 3`

5. **push 여부**: 명시적 지시 없으면 로컬 커밋까지만.

### 톤·스타일 규칙

- "~습니다" 문체 대신 명사형·간결체 ("결정함", "~확정", "~추가")
- 불필요한 형용사·감탄 금지 ("훌륭한", "멋진" 등)
- 코드 라인은 필요할 때만 인라인 백틱으로 간단히. 큰 코드 블록은 본문이 아닌 
  `firmware/`, `web/` 파일에 속하므로 일지에 복붙하지 않는다.
- "왜 이렇게 했는가"가 "무엇을 했는가"보다 우선. 단순 작업 나열은 피하고 
  의사결정·근거 기록에 집중.

### 주의사항

- **민감 정보 금지**: API 키, 개인 정보, 학생 실제 데이터 등은 일지에 적지 
  않는다. 대신 "API 키는 sessionStorage에 저장" 같은 설계 수준 기술만 허용.
- **자랑/홍보 톤 금지**: 논문용 1차 자료이므로 중립적 기술.
- **추측과 사실 구분**: 확실하지 않은 날짜·숫자는 "(추정)", "(대략)" 명시.
- **기존 일지 내용은 함부로 삭제하지 말 것**: 오타 교정·사실 오류 수정은 
  가능하나, 과거 의사결정 기록 자체는 보존. 번복된 결정은 "(후일 Phase N에서 
  재결정, 해당 Phase 참조)" 같은 주석으로 처리.

### 예시 미니 블록 (참고용)

```markdown
### 2026-04-23 — Step 3-1 펌웨어 에뮬레이터 스켈레톤

#### 한 일
- `tools/firmware-emulator/` 신규 (Node.js + ws)
- WebSocket 서버 `ws://localhost:8787`, 연결 시 v1.1 hello 1회 전송
- 로컬 연결·수신 테스트 통과

#### 결정: com0com 대신 WebSocket 에뮬레이션

**배경**: 실물 ESP32 도착 전, 브라우저 `WebSerialSensorSource` 개발에 필요한 
실시간 데이터 소스 확보.

**결정**: Node.js + WebSocket 서버 방식. 가상 시리얼 포트(com0com)는 배제.

**근거**: com0com은 Windows 커널 드라이버 + Secure Boot/TESTSIGNING 조정 필요. 
설치 실패 보고 다수(Win11). WebSocket은 npm install 한 번으로 설정 완료. 
파서 로직을 공통 모듈로 분리하면 실물 전환 시 `WebSerialSensorSource`에서 
재사용 가능하므로 "실물 전환 시 코드 수정 0" 목표도 유지.

**배제된 대안**:
- com0com 가상 시리얼: 설치 장벽 과대, 1회성 투자 대비 효용 낮음
- 파일 replay: 실시간성 부재로 UI 테스트 한계
```

### 작성 주기 권장

매 Step/커밋마다 일지를 쓰는 것은 비효율적. 아래 시점 중 **먼저 도래하는 쪽**
에서 일지를 작성한다.

1. **의미 있는 마일스톤 완료 시** (우선)
   - 예: "에뮬레이터 완성"(Step 3-1~3-3 묶음), "브라우저 통합 완성"
     (Step 3-4~3-5 묶음), "실물 검증 완료"(Step 3-6)
   - 즉, 여러 Step을 하나의 **개념적 단위**로 묶어 기록
2. **세션 종료 시** (보조)
   - 하루 작업을 마무리할 때 — 다음 세션에 기억이 희석되는 것을 방지
3. **Phase 전체 완료 시** (최후)
   - 위 두 시점을 놓쳤다면 최소한 Phase 전환 시 일지 정리

한 Phase당 일지 기입 횟수는 보통 **2~3회**가 적정. 너무 잦으면 쓰레기 커밋,
너무 드물면 세부 기억 손실.

### 일지 초안 작성 원칙 (Claude 대화 ↔ CC 분업)

- 기술적 **사실**(파일명, 구현 내용, 커밋 해시 등)은 CC가 직접 채울 수 있음
- 그러나 **의사결정의 근거·배제된 대안·막힌 지점의 맥락**은 일반적으로 
  Claude 대화 창에서 논의된 내용이므로, 해당 부분은 Claude 대화 쪽 초안을
  받아 CC가 반영하는 것이 정확하다
- 따라서 일지 요청이 "의사결정 기록 포함"이면 Claude 대화로 초안을 먼저 
  받고, 순수 구현 기록이면 CC가 `git log` 기반으로 자체 작성 가능

---

## 파일 관리 규칙 (Claude Code 필수 준수)

### 1. 파일명 — 날짜는 파일명이 아니라 파일 안에 둔다
- *살아있는 마스터*(계속 덮어쓰며 갱신하는 현행본 1개)는 **무날짜 고정 이름**으로
  만든다. 파일 맨 위에 `> 최종 수정: YYYY-MM-DD` 줄 + 변경 이력을 둔다.
  파일명에 날짜를 넣지 마라 — 수정마다 그 파일을 가리키는 모든 참조가 깨진다.
  "언제 고쳤나"의 정확한 기록은 git이 한다.
- *버전 스냅샷*(각 버전을 동결 보존)만 `YYYY-MM-DD_종류_vN.md`로 만든다
  (날짜=생성일·불변).
- *생성물*(다른 파일에서 빌드한 것)은 무날짜 고정 이름으로 덮어쓴다.
- 대체된 옛 버전은 즉시 `history/<종류>/`로 옮긴다.

### 2. 동기화 레지스트리를 유지한다 — 일부만 수정 금지
- 이 CLAUDE.md에 "함께 움직이는 파일" 클러스터 표를 유지하라.
- *새 파일을 만들 때*: 그 파일과 관련된 파일들을 클러스터 표에 등록하라.
- *파일을 수정할 때*: 먼저 표에서 그 파일이 속한 클러스터를 확인하라.
  **단, 형제 파일을 즉시 함께 고치지 않는다**(아래 "지연 동기화" 참조).
  요청받은 파일만 고치고, 재동기 필요분은 `SYNC-PENDING.md`에 기록한다.
- 동기화가 깨질 수 있는 쌍(원본↔사본, 소스↔생성물, 문서↔인덱스)은 반드시
  한 클러스터로 묶어라.
- 수정·생성 후, 그 변경이 다른 클러스터 파일에 영향을 주는지 스스로 점검하고
  영향 받는 형제 파일을 `SYNC-PENDING.md` 대기 목록에 한 줄로 기록하라.

### 3. 지연 동기화 (deferred sync) — timing 규칙

> **이 timing 규칙은 위·아래의 "즉시·전부·한 번에 갱신" 류 문구보다 우선한다.**
> *무엇이 연동되나(grouping)*는 위 클러스터 표를 그대로 따르고,
> *언제 전파하나(timing)*만 지연한다. 목적: 한 곳을 고칠 때마다 형제 전부를
> 재빌드하는 토큰 낭비를 막고, 전파를 `/sync` 시점에 일괄 처리.

- **평소(기록만)**: 클러스터 파일을 수정하면 형제 파일을 즉시 고치지 않고,
  요청받은 파일만 고친다. 그 직후 루트 `SYNC-PENDING.md` 대기 목록에
  "무엇을 고쳤고 / 어떤 클러스터의 / 무엇이 재동기 필요한지" 한 줄을
  **자동 기록**한다(파일 없으면 생성). 기록은 항상·자동.
- **전파(일괄)**: 형제 일치·생성물 재빌드·검증은 사용자가 **`/sync`**(또는
  "동기화"·"연동 고쳐")라고 할 때만 클러스터 정의대로 *일괄* 수행하고,
  완료한 항목의 줄을 `SYNC-PENDING.md`에서 삭제한다.
- **반쪽 커밋 방지**: 커밋/푸시·세션 종료 직전 `SYNC-PENDING.md`의 대기 목록이
  비어있지 않으면 **경고하고 먼저 `/sync`를 권한다**.
- 절차 상세는 `.claude/commands/sync.md`(슬래시 명령 `/sync`) 참조.

### 동기화 클러스터 표

> 범례: `→` = 소스에서 빌드(생성물), `↔` = 상호 동기화 필요. ✅=참조 검증됨,
> ◻=명명·구조로 추론(작업 시 확인). 최종 갱신: 2026-05-23.

| 클러스터 | 함께 움직이는 파일 | 동기화 트리거 |
|---|---|---|
| ✅ 검사지(measure) | `measure/검사지-최종문항.html`(정본) → `make_docx.py` → `검사지-최종문항.docx`(생성물); `검사지-배포본.html`(학생용) → `google-form-script.gs`(구글폼); `검사지-종합.html`(설계·링크 허브); `검사지-수정메모.md`(이력·대상 포인터); `docs/10-dev-journal.md`(Phase 7) | 문항이 바뀌면: 정본 수정 → `make_docx.py` 재실행으로 docx 갱신 → 배포본·gs 스크립트·종합문서 링크/문항 동기화 → 수정메모·일지 기록 |
| ✅ 검사지 history | `measure/history/검사지-{설계계획·보강안·전체문항·개정판·수정방향}.html`(동결 보존) | 정본이 새 버전으로 대체될 때만 옛 버전을 이리로 이동(내용 수정 금지) |
| ◻ 펌웨어 프로토콜 | `firmware/boyle/boyle.ino` ↔ `web/js/protocol.js` ↔ `web/js/serial.js` ↔ `tools/firmware-emulator/{emulator,baseline}.js` ↔ `docs/12-protocol-v1.2.md` · `docs/05-data-format.md` | 프로토콜 버전/프레임 포맷 변경 시 펌웨어·웹 파서·에뮬레이터·문서 전부 동시 갱신 |
| ◻ web 파라미터 | `web/config/params.json` ↔ `docs/15-params-config-guide.md` ↔ 소비처(`web/js/main.js` 등) | params 키 추가/변경 시 가이드 문서·소비 코드 동시 갱신 |
| ◻ 프로젝트 인덱스 | `README.md`(페이지 구성·기능) ↔ `docs/00-project-overview.md` ↔ `docs/06-project-status.md` ↔ `docs/09-roadmap.md` | Phase 전환·기능 추가 시 README와 상태/로드맵 문서 동시 갱신 |
